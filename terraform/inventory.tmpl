[nodes]
${node1-dns} ansible_host=${node1-ip} # id: ${node1-id}
${node2-dns} ansible_host=${node2-ip} # id: ${node2-id}
${node3-dns} ansible_host=${node3-ip} # id: ${node3-id}
${node4-dns} ansible_host=${node4-ip} # id: ${node4-id}
${node5-dns} ansible_host=${node5-ip} # id: ${node5-id}
${node6-dns} ansible_host=${node6-ip} # id: ${node6-id}
